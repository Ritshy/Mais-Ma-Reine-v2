using System.Collections;
using System.Collections.Generic;
using Cinemachine;
using UnityEngine;

/*public class CameraFollow : MonoBehaviour
{
    public Raycast myRaycast;

    private CinemachineVirtualCamera myCM;

    // Start is called before the first frame update
    void Start()
    {
        myCM = gameObject.GetComponent<CinemachineVirtualCamera>();
    }

    // Update is called once per frame
    void Update()
    {
        if (myRaycast.followTarget != null)
        {
            myCM.Follow = myRaycast.followTarget.transform;
            //myCM.Priority = 11;
        }
        else
        {
            myCM.Follow = null;
            //myCM.Priority = 10;
        }
    }
}*/
